# 🌐 Публичный доступ к Web Server Interceptor

Инструкция по настройке доступа к серверу не только локально, но и извне.

## 🚀 Быстрая настройка

### Автоматическая настройка

```bash
# Запустите скрипт для получения всех адресов
./setup_public_access.sh 5000
```

Скрипт покажет:
- Локальный IP адрес
- Публичный IP адрес
- Tor .onion адрес (если настроен)
- Инструкции по настройке

## 📋 Способы доступа

### 1. Локальная сеть (WiFi/LAN)

**Самый простой способ** - доступ с других устройств в той же сети.

#### Шаг 1: Узнайте локальный IP
```bash
hostname -I
# или
ip addr show | grep "inet "
```

#### Шаг 2: Откройте в браузере
```
http://[локальный-IP]:5000/mask
```

**Пример:**
```
http://192.168.1.100:5000/mask
http://192.168.1.100:5000/intercept
http://192.168.1.100:5000/admin/reports
```

#### Шаг 3: С мобильного устройства
- Подключите телефон к той же WiFi сети
- Откройте браузер
- Введите: `http://[локальный-IP]:5000/mask`

### 2. Публичный IP (через интернет)

Доступ из любой точки мира через публичный IP адрес.

#### Шаг 1: Узнайте публичный IP
```bash
curl https://api.ipify.org
# или
curl https://ifconfig.me/ip
```

#### Шаг 2: Настройте Port Forwarding в роутере

1. Войдите в настройки роутера (обычно `192.168.1.1` или `192.168.0.1`)
2. Найдите раздел "Port Forwarding" или "Виртуальные серверы"
3. Добавьте правило:
   - **Имя**: Web Interceptor
   - **Внешний порт**: 5000
   - **Внутренний IP**: IP адрес вашего сервера (узнайте через `hostname -I`)
   - **Внутренний порт**: 5000
   - **Протокол**: TCP
4. Сохраните настройки

#### Шаг 3: Откройте порт в firewall
```bash
sudo ./setup_firewall.sh 5000
```

#### Шаг 4: Доступ
```
http://[публичный-IP]:5000/mask
```

**Пример:**
```
http://123.45.67.89:5000/mask
http://123.45.67.89:5000/intercept
http://123.45.67.89:5000/admin/reports
```

### 3. Tor Hidden Service (.onion) ⭐ Рекомендуется

**Самый безопасный способ** - не требует открытия портов.

#### Шаг 1: Запустите Tor
```bash
python3 tor_setup.py start
```

#### Шаг 2: Получите .onion адрес
```bash
python3 view_logs.py onion
# или
python3 tor_setup.py hidden
```

#### Шаг 3: Доступ через Tor Browser
```
http://[onion-address].onion/mask
```

**Преимущества:**
- ✅ Не требует открытия портов
- ✅ Полная анонимность
- ✅ Работает из любой сети
- ✅ Защита от DDoS

### 4. Динамический DNS (опционально)

Если у вас динамический IP, используйте сервисы типа:
- **No-IP**: https://www.noip.com
- **DuckDNS**: https://www.duckdns.org
- **FreeDNS**: https://freedns.afraid.org

#### Настройка DuckDNS (пример)

1. Зарегистрируйтесь на https://www.duckdns.org
2. Создайте поддомен (например: `myinterceptor`)
3. Установите обновление IP:
```bash
# Установка
sudo apt install curl

# Обновление IP (добавьте в cron)
*/5 * * * * curl "https://www.duckdns.org/update?domains=myinterceptor&token=YOUR_TOKEN&ip="
```

4. Доступ:
```
http://myinterceptor.duckdns.org:5000/mask
```

## 🔧 Настройка firewall

### Автоматическая настройка
```bash
sudo ./setup_firewall.sh 5000
```

### Ручная настройка

#### UFW (Ubuntu/Debian/Kali)
```bash
sudo ufw allow 5000/tcp
sudo ufw status
```

#### Firewalld (CentOS/RHEL/Fedora)
```bash
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
sudo firewall-cmd --list-ports
```

#### iptables (универсальный)
```bash
sudo iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
sudo iptables-save > /etc/iptables/rules.v4
```

## 🖥️ Настройка для виртуальной машины

### Parallels Desktop (macOS)

1. Настройки VM → Сеть → Дополнительно
2. Настройки портов → Добавить
3. Настройки:
   - Имя: Web Interceptor
   - Протокол: TCP
   - Порт хоста: 5000
   - Порт гостя: 5000
   - IP гостя: IP адрес VM

4. Доступ с хоста:
```
http://localhost:5000/mask
```

5. Доступ извне:
```
http://[IP-хоста]:5000/mask
```

### VMware / VirtualBox

Аналогично Parallels - настройте Port Forwarding в настройках VM.

## 🔍 Проверка доступности

### Локальная сеть
```bash
# С другого устройства в той же сети
curl http://[локальный-IP]:5000/

# Проверка порта
nc -zv [локальный-IP] 5000
```

### Публичный IP
```bash
# С внешнего устройства
curl http://[публичный-IP]:5000/

# Онлайн проверка портов
# https://www.yougetsignal.com/tools/open-ports/
# https://canyouseeme.org/
```

### Tor
```bash
# Через Tor Browser
# Откройте: http://[onion-address].onion/mask

# Через curl с Tor прокси
curl --socks5 127.0.0.1:9050 http://[onion-address].onion/mask
```

## 📱 Доступ с мобильного устройства

### В локальной сети

1. Убедитесь, что телефон в той же WiFi сети
2. Узнайте IP сервера: `hostname -I`
3. Откройте в браузере телефона:
```
http://[локальный-IP]:5000/mask
```

### Через интернет

1. Настройте port forwarding в роутере
2. Узнайте публичный IP: `curl ifconfig.me`
3. Откройте в браузере:
```
http://[публичный-IP]:5000/mask
```

### Через Tor

1. Установите Tor Browser на Android
2. Получите .onion адрес
3. Откройте:
```
http://[onion-address].onion/mask
```

## 🔒 Безопасность

### ⚠️ ВАЖНО: Меры безопасности

1. **Ограничьте доступ к админ панели**
   ```bash
   # В app.py можно добавить IP whitelist
   # Или использовать VPN
   ```

2. **Используйте HTTPS** (рекомендуется)
   - Настройте SSL сертификат
   - Используйте reverse proxy (nginx)

3. **Измените порт по умолчанию**
   ```bash
   export FLASK_PORT=8080
   ./run.sh start
   ```

4. **Используйте Tor** для максимальной анонимности
   - Не требует открытия портов
   - Полная анонимность

5. **Настройте fail2ban** для защиты от брутфорса
   ```bash
   sudo apt install fail2ban
   ```

## 🐛 Решение проблем

### Порт не открыт
```bash
# Проверка firewall
sudo ufw status
# или
sudo firewall-cmd --list-ports

# Проверка прослушивания
sudo netstat -tuln | grep 5000
# или
sudo ss -tuln | grep 5000
```

### Не могу подключиться извне

1. Проверьте firewall правила
2. Проверьте port forwarding в роутере
3. Проверьте, что сервер слушает на 0.0.0.0:
   ```bash
   sudo netstat -tuln | grep 5000
   # Должно быть: 0.0.0.0:5000
   ```

### VM не доступна извне

1. Проверьте настройки сети VM (NAT/Bridged)
2. Настройте port forwarding в настройках VM
3. Используйте Bridged режим для прямого доступа

### Динамический IP меняется

Используйте динамический DNS (DuckDNS, No-IP) или Tor .onion адрес.

## 📊 Примеры использования

### Сценарий 1: Демонстрация в классе

1. Запустите сервер на ноутбуке
2. Узнайте локальный IP: `hostname -I`
3. Поделитесь IP с участниками
4. Все подключаются: `http://[IP]:5000/mask`

### Сценарий 2: Удаленный доступ

1. Настройте port forwarding в роутере
2. Узнайте публичный IP: `curl ifconfig.me`
3. Откройте из любой точки мира: `http://[IP]:5000/mask`

### Сценарий 3: Максимальная анонимность

1. Запустите Tor: `python3 tor_setup.py start`
2. Получите .onion адрес: `python3 view_logs.py onion`
3. Поделитесь .onion адресом
4. Доступ через Tor Browser: `http://[onion].onion/mask`

## 🎯 Рекомендации

### Для локального использования
- Используйте локальный IP адрес
- Не требует настройки роутера
- Быстро и просто

### Для публичного доступа
- Используйте Tor .onion адрес (рекомендуется)
- Или настройте port forwarding + публичный IP
- Обязательно настройте firewall

### Для максимальной безопасности
- Используйте только Tor
- Не открывайте порты в firewall
- Используйте VPN для админ панели

---

**⚠️ Помните:** Используйте только в образовательных целях и в изолированной среде!

