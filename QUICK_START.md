# 🚀 Быстрый старт - Полная система перехвата

## 📋 Что было создано

1. **🎭 Маскировочный сайт** - Выглядит как обычный новостной сайт
2. **🔄 Автоматическое перенаправление** - Через 2-3 секунды на страницу перехвата
3. **📊 Шуточный отчет** - Показывает пользователю "отчет о перехвате" в стиле хакера
4. **💾 Сохранение данных** - Все данные сохраняются локально в базу и файлы
5. **🧅 Tor интеграция** - Работа через .onion адрес для анонимности

## 🎯 Как использовать

### Шаг 1: Запуск сервера

```bash
# Запуск всех сервисов
./run.sh start
```

### Шаг 2: Настройка Tor (опционально, но рекомендуется)

```bash
# Настройка перенаправления через Tor
./setup_tor_redirect.sh

# Получение .onion адреса
python3 view_logs.py onion
```

### Шаг 3: Доступ к маскировочному сайту

#### Вариант 1: Через HTTP (локально)
```
http://localhost:5000/mask
```

#### Вариант 2: Через Tor (.onion)
```
http://[onion-address].onion/mask
```

### Шаг 4: Что происходит

1. Пользователь открывает маскировочный сайт
2. Видит обычный новостной сайт
3. Через 2-3 секунды автоматически перенаправляется на `/intercept`
4. Видит шуточный отчет о "перехвате" со всеми данными
5. Все данные сохраняются в базу данных

## 📊 Просмотр результатов

### Админ панель
```
http://localhost:5000/admin/reports
```

### Через утилиту
```bash
# Последние перехваты
python3 view_logs.py intercepts 20

# Статистика
python3 view_logs.py stats
```

### Через API
```bash
curl http://localhost:5000/admin/api/reports | jq
```

## 🎨 Кастомизация

### Изменить время перенаправления

В `templates/mask_site.html`:
```javascript
setTimeout(function() {
    window.location.href = '/intercept?ref=mask_site';
}, 2000 + Math.random() * 1000); // Измените 2000 на нужное время (в мс)
```

### Изменить дизайн маскировочного сайта

Отредактируйте `templates/mask_site.html`:
- Название сайта
- Статьи
- Цвета
- Контент

### Изменить отчет о перехвате

Отредактируйте `templates/caught_report.html`:
- Текст предупреждений
- Стиль (цвета, анимации)
- Отображаемую информацию

## 🔧 Настройка для внешнего доступа

### Через HTTP

```bash
# Настройка firewall
sudo ./setup_firewall.sh 5000

# Узнать IP адрес
hostname -I

# Доступ: http://[IP]:5000/mask
```

### Через Tor (рекомендуется)

```bash
# Получить .onion адрес
python3 view_logs.py onion

# Доступ: http://[onion-address].onion/mask
```

## 📝 Примеры использования

### Сценарий 1: Демонстрация

1. Запустите сервер
2. Откройте `http://localhost:5000/mask`
3. Покажите как работает перенаправление
4. Продемонстрируйте собранные данные

### Сценарий 2: Через Tor

1. Запустите сервер с Tor
2. Получите .onion адрес
3. Поделитесь адресом
4. Соберите данные о подключениях

### Сценарий 3: Образовательный

1. Покажите маскировочный сайт
2. Объясните как работает перехват
3. Продемонстрируйте важность защиты приватности
4. Покажите рекомендации по безопасности

## ⚠️ Важно

- Используйте только в образовательных целях
- Не используйте на реальных пользователях без разрешения
- Соблюдайте законы вашей страны
- Используйте в изолированной среде

## 📚 Дополнительная документация

- [README.md](README.md) - Основная документация
- [MASK_AND_REDIRECT.md](MASK_AND_REDIRECT.md) - Детали маскировочного сайта
- [EXTERNAL_ACCESS.md](EXTERNAL_ACCESS.md) - Настройка внешнего доступа
- [DOCKER_AND_LOGGING.md](DOCKER_AND_LOGGING.md) - Docker и логирование

---

**🎓 Образовательный проект по кибербезопасности**

