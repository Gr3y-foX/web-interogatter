# 🍓 Установка на Raspberry Pi 4

Полная инструкция по установке и настройке Web Server Interceptor на Raspberry Pi 4.

## 📋 Требования

### Оборудование
- **Raspberry Pi 4** (рекомендуется 4GB RAM или больше)
- **MicroSD карта** минимум 16GB (рекомендуется 32GB+)
- **Блок питания** 5V 3A (официальный или совместимый)
- **Сетевое подключение** (Ethernet или Wi-Fi)

### Программное обеспечение
- **Raspberry Pi OS** (64-bit) - рекомендуется Bullseye или новее
- **Docker** и **Docker Compose**
- **Git** (для клонирования репозитория)

## 🚀 Быстрая установка

### 1. Подготовка Raspberry Pi OS

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
sudo apt install -y git curl wget
```

### 2. Установка Docker

```bash
# Автоматическая установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER

# Установка Docker Compose (если не установлен)
sudo apt install -y docker-compose-plugin

# Перезагрузка для применения изменений
sudo reboot
```

После перезагрузки войдите снова в систему.

### 3. Клонирование проекта

```bash
# Переход в домашнюю директорию
cd ~

# Клонирование репозитория
git clone <repository-url> web-server-intercepter
cd web-server-intercepter
```

### 4. Автоматическая настройка

```bash
# Запуск скрипта автоматической настройки
chmod +x setup_raspberry.sh
./setup_raspberry.sh
```

Скрипт автоматически:
- Проверит все зависимости
- Настроит Docker
- Создаст необходимые директории
- Настроит права доступа
- Оптимизирует систему для работы

### 5. Запуск приложения

```bash
# Запуск основных сервисов
./raspberry-run.sh start

# Или запуск всех сервисов
./raspberry-run.sh start-full
```

## 📖 Детальная установка

### Шаг 1: Установка Raspberry Pi OS

1. Скачайте **Raspberry Pi OS (64-bit)** с официального сайта
2. Запишите образ на MicroSD карту с помощью Raspberry Pi Imager
3. Включите SSH в настройках (для удаленного доступа)
4. Вставьте карту в Raspberry Pi и включите устройство

### Шаг 2: Первоначальная настройка

```bash
# Подключение по SSH (если используете удаленный доступ)
ssh pi@raspberrypi.local

# Изменение пароля (рекомендуется)
passwd

# Настройка локали
sudo raspi-config
# Выберите: Localisation Options -> Change Locale -> en_US.UTF-8
```

### Шаг 3: Установка Docker вручную

Если автоматическая установка не сработала:

```bash
# Удаление старых версий
sudo apt remove docker docker-engine docker.io containerd runc

# Установка зависимостей
sudo apt install -y \
    ca-certificates \
    curl \
    gnupg \
    lsb-release

# Добавление официального GPG ключа Docker
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Добавление репозитория
echo \
  "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian \
  $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установка Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Проверка установки
docker --version
docker compose version
```

### Шаг 4: Оптимизация для Raspberry Pi

```bash
# Увеличение swap (если RAM < 4GB)
sudo dphys-swapfile swapoff
sudo nano /etc/dphys-swapfile
# Измените CONF_SWAPSIZE=2048 (для 2GB swap)
sudo dphys-swapfile setup
sudo dphys-swapfile swapon

# Оптимизация файловой системы
sudo tune2fs -o journal_data_writeback /dev/mmcblk0p2

# Отключение ненужных сервисов (опционально)
sudo systemctl disable bluetooth
sudo systemctl disable avahi-daemon
```

### Шаг 5: Настройка проекта

```bash
# Клонирование проекта
cd ~
git clone <repository-url> web-server-intercepter
cd web-server-intercepter

# Настройка прав
chmod +x *.sh
chmod +x docker/entrypoint.sh

# Создание директорий
mkdir -p data reports logs
```

## 🎮 Использование

### Управление сервисами

```bash
# Запуск основных сервисов (рекомендуется для Raspberry Pi)
./raspberry-run.sh start

# Запуск всех сервисов
./raspberry-run.sh start-full

# Запуск с Nginx прокси
./raspberry-run.sh start --profile nginx

# Остановка
./raspberry-run.sh stop

# Перезапуск
./raspberry-run.sh restart

# Статус
./raspberry-run.sh status

# Логи
./raspberry-run.sh logs
```

### Доступ к интерфейсам

После запуска сервисы будут доступны по следующим адресам:

- **Основной сайт**: `http://raspberrypi.local:5000` или `http://[IP-адрес]:5000`
- **Админ панель**: `http://raspberrypi.local:5000/admin/reports`
- **API**: `http://raspberrypi.local:5000/admin/api/reports`
- **SQLite Web** (если включен): `http://raspberrypi.local:8080`

### Получение .onion адреса

```bash
# Получение Tor Hidden Service адреса
./raspberry-run.sh onion

# Ожидание может занять 1-2 минуты после первого запуска
```

## 🔧 Настройка и оптимизация

### Ограничение ресурсов

Raspberry Pi имеет ограниченные ресурсы. В `docker-compose.raspberry.yml` уже настроены ограничения:

- **interceptor**: максимум 1GB RAM, 2 CPU ядра
- **tor-relay**: максимум 512MB RAM, 1 CPU ядро
- **nginx-proxy**: максимум 128MB RAM, 0.5 CPU ядра

Вы можете изменить эти значения в файле `docker-compose.raspberry.yml`.

### Мониторинг ресурсов

```bash
# Просмотр использования ресурсов
docker stats

# Просмотр использования диска
df -h

# Просмотр использования памяти
free -h

# Температура процессора
vcgencmd measure_temp
```

### Оптимизация производительности

1. **Используйте быструю MicroSD карту** (Class 10 или выше, лучше A2)
2. **Используйте USB SSD** для лучшей производительности
3. **Охлаждение**: установите радиаторы или вентилятор
4. **Overclocking** (опционально, на свой риск):
   ```bash
   sudo raspi-config
   # Advanced Options -> Overclock -> Pi4
   ```

## 🌐 Настройка сетевого доступа

### Локальная сеть

По умолчанию сервисы доступны в локальной сети. Узнайте IP адрес:

```bash
hostname -I
```

Затем откройте в браузере: `http://[IP-адрес]:5000`

### Публичный доступ через Tor

Tor Hidden Service автоматически создается при запуске. Получите адрес:

```bash
./raspberry-run.sh onion
```

### Порт-форвардинг (опционально)

Если хотите открыть доступ через интернет:

```bash
# Настройка firewall
sudo ./setup_firewall.sh 5000

# Настройка публичного доступа
./setup_public_access.sh 5000
```

**⚠️ ВНИМАНИЕ**: Открытие портов в интернет может быть небезопасно. Используйте Tor Hidden Service вместо этого.

## 🐛 Решение проблем

### Проблема: Docker не запускается

```bash
# Проверка статуса Docker
sudo systemctl status docker

# Запуск Docker
sudo systemctl start docker
sudo systemctl enable docker

# Проверка прав доступа
groups $USER
# Должна быть группа 'docker'
```

### Проблема: Недостаточно памяти

```bash
# Увеличение swap
sudo dphys-swapfile swapoff
sudo nano /etc/dphys-swapfile
# CONF_SWAPSIZE=2048
sudo dphys-swapfile setup
sudo dphys-swapfile swapon

# Очистка Docker
docker system prune -a
```

### Проблема: Медленная работа

1. Проверьте температуру: `vcgencmd measure_temp`
2. Убедитесь, что используется быстрая MicroSD карта
3. Рассмотрите использование USB SSD
4. Уменьшите количество запущенных сервисов

### Проблема: Контейнеры не запускаются

```bash
# Просмотр логов
./raspberry-run.sh logs

# Проверка статуса
./raspberry-run.sh status

# Пересборка образов
./raspberry-run.sh build
```

### Проблема: Tor не создает Hidden Service

```bash
# Проверка логов Tor
docker logs web-interceptor-raspberry | grep -i tor

# Ручная проверка
docker exec web-interceptor-raspberry ls -la /var/lib/tor-interceptor/hidden_service/

# Перезапуск Tor
docker exec web-interceptor-raspberry pkill tor
```

## 📊 Мониторинг и обслуживание

### Автоматический запуск при загрузке

```bash
# Создание systemd сервиса
sudo nano /etc/systemd/system/web-interceptor.service
```

Добавьте:

```ini
[Unit]
Description=Web Server Interceptor
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/pi/web-server-intercepter
ExecStart=/home/pi/web-server-intercepter/raspberry-run.sh start
ExecStop=/home/pi/web-server-intercepter/raspberry-run.sh stop
User=pi
Group=pi

[Install]
WantedBy=multi-user.target
```

Активация:

```bash
sudo systemctl daemon-reload
sudo systemctl enable web-interceptor.service
sudo systemctl start web-interceptor.service
```

### Резервное копирование

```bash
# Создание резервной копии данных
./raspberry-run.sh export

# Резервная копия вручную
tar -czf backup_$(date +%Y%m%d).tar.gz data/ reports/ logs/
```

### Обновление

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Обновление Docker образов
./raspberry-run.sh update

# Обновление проекта
cd ~/web-server-intercepter
git pull
./raspberry-run.sh build
./raspberry-run.sh restart
```

## 🔒 Безопасность

### Рекомендации

1. **Измените пароль по умолчанию**: `passwd`
2. **Настройте firewall**: `sudo ufw enable`
3. **Используйте SSH ключи** вместо паролей
4. **Регулярно обновляйте систему**: `sudo apt update && sudo apt upgrade`
5. **Используйте Tor Hidden Service** для публичного доступа
6. **Не открывайте порты в интернет** без необходимости

### Настройка firewall

```bash
# Установка UFW
sudo apt install -y ufw

# Разрешение SSH
sudo ufw allow 22/tcp

# Разрешение локального доступа к приложению
sudo ufw allow from 192.168.0.0/16 to any port 5000

# Включение firewall
sudo ufw enable
```

## 📝 Дополнительные ресурсы

- [Официальная документация Raspberry Pi](https://www.raspberrypi.org/documentation/)
- [Docker для ARM](https://docs.docker.com/engine/install/debian/)
- [Tor документация](https://2019.www.torproject.org/docs/documentation.html.en)

## ⚠️ Важные замечания

1. **Raspberry Pi 4** рекомендуется с **4GB RAM** или больше
2. Используйте **быструю MicroSD карту** (Class 10, A2) или **USB SSD**
3. Обеспечьте **хорошее охлаждение** для стабильной работы
4. Для production использования рассмотрите **Raspberry Pi 4 с 8GB RAM**
5. Регулярно делайте **резервные копии** данных

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте логи: `./raspberry-run.sh logs`
2. Проверьте статус: `./raspberry-run.sh status`
3. Проверьте документацию в `README.md`
4. Создайте issue в репозитории проекта

---

**🎓 Образовательный проект по кибербезопасности**  
*Используйте ответственно и в соответствии с законами*

