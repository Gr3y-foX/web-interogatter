# 🌐 Настройка внешнего доступа

Инструкция по настройке доступа к Web Server Interceptor извне (не только localhost).

## 📋 Предварительные требования

1. Сервер должен быть запущен и слушать на `0.0.0.0` (уже настроено по умолчанию)
2. Firewall должен разрешать входящие подключения на порт 5000
3. Если используется VM, нужно настроить port forwarding
4. Если используется роутер, нужно настроить port forwarding

## 🔧 Быстрая настройка

### 1. Автоматическая настройка firewall

```bash
# Настройка firewall для порта 5000
sudo ./setup_firewall.sh 5000

# Или для другого порта
sudo ./setup_firewall.sh 8080
```

### 2. Ручная настройка firewall

#### UFW (Ubuntu/Debian/Kali)
```bash
sudo ufw allow 5000/tcp
sudo ufw status
```

#### Firewalld (CentOS/RHEL/Fedora)
```bash
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
sudo firewall-cmd --list-ports
```

#### iptables (универсальный)
```bash
sudo iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
sudo iptables-save > /etc/iptables/rules.v4
```

## 🖥️ Настройка для виртуальной машины

### Parallels Desktop (macOS)
1. Откройте настройки VM
2. Перейдите в "Сеть" → "Дополнительно"
3. Нажмите "Настройки портов"
4. Добавьте правило:
   - Имя: Web Interceptor
   - Протокол: TCP
   - Порт хоста: 5000
   - Порт гостя: 5000
   - IP гостя: IP адрес VM (например, 10.211.55.14)

### VMware
1. VM → Settings → Network Adapter
2. Advanced → Port Forwarding
3. Add:
   - Host Port: 5000
   - Guest Port: 5000
   - Guest IP: IP адреса VM

### VirtualBox
1. VM → Settings → Network
2. Advanced → Port Forwarding
3. Add:
   - Name: Web Interceptor
   - Protocol: TCP
   - Host IP: (оставить пустым)
   - Host Port: 5000
   - Guest IP: (оставить пустым)
   - Guest Port: 5000

## 🌍 Настройка для роутера

Если сервер находится за роутером, нужно настроить port forwarding:

1. Узнайте локальный IP сервера:
   ```bash
   hostname -I
   # или
   ip addr show
   ```

2. Войдите в настройки роутера (обычно 192.168.1.1 или 192.168.0.1)

3. Найдите раздел "Port Forwarding" или "Виртуальные серверы"

4. Добавьте правило:
   - Внешний порт: 5000
   - Внутренний IP: IP адрес вашего сервера
   - Внутренний порт: 5000
   - Протокол: TCP

5. Сохраните настройки

6. Узнайте публичный IP:
   ```bash
   curl ifconfig.me
   # или
   curl ipinfo.io/ip
   ```

## 🔍 Проверка доступности

### 1. Проверка локальной сети
```bash
# С другого устройства в той же сети
curl http://[локальный-IP]:5000

# Например
curl http://192.168.1.100:5000
```

### 2. Проверка извне
```bash
# С внешнего устройства
curl http://[публичный-IP]:5000

# Или через браузер
http://[публичный-IP]:5000
```

### 3. Проверка порта
```bash
# Проверка открыт ли порт
nc -zv [IP-адрес] 5000

# Или
telnet [IP-адрес] 5000
```

### 4. Онлайн проверка портов
- https://www.yougetsignal.com/tools/open-ports/
- https://canyouseeme.org/

## 🔒 Безопасность

### ⚠️ ВАЖНО: Меры безопасности

1. **Ограничьте доступ к админ панели**
   - Используйте VPN для доступа
   - Или настройте IP whitelist в firewall

2. **Используйте HTTPS** (рекомендуется)
   - Настройте SSL сертификат
   - Используйте reverse proxy (nginx)

3. **Измените порт по умолчанию**
   ```bash
   export FLASK_PORT=8080
   ./run.sh start
   ```

4. **Настройте fail2ban** для защиты от брутфорса
   ```bash
   sudo apt install fail2ban
   ```

5. **Используйте Tor Hidden Service** для максимальной анонимности
   - Доступ через .onion адрес
   - Не требует открытия портов

## 🧅 Доступ через Tor (рекомендуется)

Tor Hidden Service автоматически доступен извне без настройки firewall:

```bash
# Получить .onion адрес
python3 view_logs.py onion

# Доступ через Tor Browser
# http://[onion-address].onion
```

**Преимущества:**
- ✅ Не требует открытия портов
- ✅ Полная анонимность
- ✅ Работает из любой сети
- ✅ Не нужно настраивать firewall

## 📊 Определение адресов

При запуске сервера автоматически отображаются все доступные адреса:

```
🌐 Сетевые адреса:
   - Hostname: kali-linux-2024-2
   - Локальный IP: 192.168.1.100
   - Публичный IP: 123.45.67.89
   - Сетевые интерфейсы: 192.168.1.100 10.0.0.5

📊 Административная панель:
   - Localhost:  http://localhost:5000/admin/reports
   - Локальная сеть:  http://192.168.1.100:5000/admin/reports
   - Публичный IP:  http://123.45.67.89:5000/admin/reports
   - Tor (.onion):   http://[onion-address].onion/admin/reports
```

## 🔧 Изменение порта

### Через переменную окружения
```bash
export FLASK_PORT=8080
./run.sh start
```

### Через редактирование app.py
Измените последнюю строку:
```python
app.run(host='0.0.0.0', port=8080, debug=False, threaded=True)
```

## 🐛 Решение проблем

### Порт не открыт
```bash
# Проверка firewall
sudo ufw status
# или
sudo firewall-cmd --list-ports

# Проверка прослушивания
sudo netstat -tuln | grep 5000
# или
sudo ss -tuln | grep 5000
```

### Не могу подключиться извне
1. Проверьте firewall правила
2. Проверьте port forwarding в роутере
3. Проверьте, что сервер слушает на 0.0.0.0:
   ```bash
   sudo netstat -tuln | grep 5000
   # Должно быть: 0.0.0.0:5000
   ```

### VM не доступна извне
1. Проверьте настройки сети VM (NAT/Bridged)
2. Настройте port forwarding в настройках VM
3. Используйте Bridged режим для прямого доступа

## 📝 Примеры использования

### Доступ с мобильного устройства
```bash
# Узнайте IP сервера в локальной сети
hostname -I

# Откройте в браузере телефона (в той же WiFi сети)
http://[локальный-IP]:5000
```

### Доступ через интернет
```bash
# Узнайте публичный IP
curl ifconfig.me

# Откройте в браузере
http://[публичный-IP]:5000
```

### Доступ через Tor
```bash
# Получите .onion адрес
python3 view_logs.py onion

# Откройте в Tor Browser
# http://[onion-address].onion
```

---

**⚠️ Помните:** Используйте только в образовательных целях и в изолированной среде!

