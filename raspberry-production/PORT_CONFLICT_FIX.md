# 🔧 Исправление конфликта порта 5000

## Проблема

При запуске Docker контейнера на Raspberry Pi появляется ошибка:
```
Error response from daemon: failed to bind host port 0.0.0.0:5000/tcp: address already in use
```

Это происходит, когда:
- 🔴 Старый Docker контейнер все еще запущен
- 🔴 Python версия приложения уже работает
- 🔴 Другой процесс использует порт 5000

## 🚀 Быстрое решение

### Вариант 1: Автоматическое исправление (Рекомендуется)

```bash
cd ~/web-interogatter/raspberry-production
chmod +x fix_port_conflict.sh
./fix_port_conflict.sh
```

Если скрипт требует больше прав:
```bash
sudo ./fix_port_conflict.sh
```

### Вариант 2: Ручное исправление

#### Шаг 1: Остановить Docker контейнеры
```bash
# Остановить конкретный контейнер
docker stop web-interceptor-raspberry
docker rm -f web-interceptor-raspberry

# Или остановить все через docker-compose
cd ~/web-interogatter
docker-compose -f docker-compose.raspberry.yml down
```

#### Шаг 2: Проверить процессы на порту 5000
```bash
# Найти процесс
sudo lsof -i:5000

# Или найти PID
sudo netstat -tulpn | grep :5000

# Остановить процесс (замените PID на реальный)
sudo kill -9 PID
```

#### Шаг 3: Остановить Python версию (если запущена)
```bash
cd ~/web-interogatter/raspberry-production
./raspberry-run.sh stop
```

#### Шаг 4: Очистить PID файлы
```bash
rm -f /tmp/web-interceptor-*.pid
```

## 🔍 Проверка состояния

### Проверить Docker контейнеры
```bash
docker ps -a | grep interceptor
```

### Проверить порт 5000
```bash
sudo lsof -i:5000
# Если ничего не выводится - порт свободен ✅
```

### Проверить процессы приложения
```bash
ps aux | grep "app.py\|flask"
```

## ▶️ После исправления

### Запуск через Python (без Docker)
```bash
cd ~/web-interogatter/raspberry-production
./raspberry-run.sh start
```

### Запуск через Docker
```bash
cd ~/web-interogatter
docker-compose -f docker-compose.raspberry.yml up -d
```

## ⚠️ Важное предупреждение

**НЕ запускайте одновременно:**
- ❌ Docker версию И Python версию
- ❌ Несколько экземпляров Docker контейнера

**Выберите один способ запуска:**
- ✅ Только Docker (`docker-compose up`)
- ✅ Только Python (`./raspberry-run.sh start`)

## 🐛 Дополнительная диагностика

### Если порт все еще занят после очистки:

```bash
# Найти все процессы на порту 5000
sudo lsof -i:5000 -t | xargs -r sudo kill -9

# Проверить все Docker контейнеры
docker ps -a

# Удалить ВСЕ остановленные контейнеры
docker container prune -f

# Проверить Docker сети
docker network ls | grep interceptor
docker network rm interceptor_network 2>/dev/null || true
```

### Логи для диагностики:

```bash
# Docker логи
docker logs web-interceptor-raspberry

# Python логи
tail -f ~/web-interogatter/logs/interceptor.log
tail -f ~/web-interogatter/logs/errors.log
```

## 📝 Память о проблеме

Первое предупреждение:
```
Your kernel does not support memory soft limit capabilities or the cgroup is not mounted
```
- Это **НЕ критично** - просто предупреждение о cgroup лимитах
- Docker будет работать без ограничений памяти

Основная проблема:
```
failed to bind host port 0.0.0.0:5000/tcp: address already in use
```
- **Это критично** - порт занят, нужно освободить

## 🎯 Рекомендации

1. **Для разработки**: используйте Python версию (`./raspberry-run.sh`)
   - Быстрее запускается
   - Меньше потребляет ресурсов
   - Проще отладка

2. **Для продакшена**: используйте Docker версию (`docker-compose`)
   - Изолированная среда
   - Автоматический перезапуск
   - Проще управление зависимостями

3. **Всегда останавливайте** предыдущий экземпляр перед запуском нового

## 🔗 Полезные команды

```bash
# Проверить статус Python версии
cd raspberry-production
./raspberry-run.sh status

# Проверить статус Docker версии
docker-compose -f docker-compose.raspberry.yml ps

# Проверить все сервисы на стандартных портах
sudo lsof -i:5000   # Flask
sudo lsof -i:80     # Nginx
sudo lsof -i:443    # Nginx SSL
sudo lsof -i:8080   # SQLite Web
sudo lsof -i:9050   # Tor SOCKS
sudo lsof -i:9051   # Tor Control
```

## ✅ Успешный запуск

После исправления вы должны увидеть:
```
✅ Порт 5000 свободен!
✅ Сервер запущен успешно
```

И можете получить доступ:
- `http://localhost:5000`
- `http://[IP_адрес_raspberry]:5000`
- `http://[onion_address].onion` (через Tor)

