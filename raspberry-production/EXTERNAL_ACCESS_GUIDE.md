# 🌐 Руководство по внешнему доступу к Raspberry Pi

## Обзор методов

Этот гайд описывает различные способы доступа к вашему Web Server Interceptor извне локальной сети для демонстрации работы проекта.

---

## 🧅 Метод 1: Tor Hidden Service (Рекомендуется для кибербезопасности)

### ✅ Преимущества:
- Полная анонимность и конфиденциальность
- Не требует публичного IP адреса
- Не нужно настраивать роутер
- Обход NAT и файрволов
- Бесплатно
- **Идеально для проектов по кибербезопасности**

### 🚀 Быстрый старт:

**1. Tor уже настроен и работает!**

Получите ваш .onion адрес:

```bash
cd ~/web-interogatter/raspberry-production
./raspberry-run.sh onion
```

Или посмотрите напрямую:

```bash
cat /tmp/tor_interceptor/hidden_service/hostname
# или
cat data/onion_address.txt
```

**2. Поделитесь адресом для демонстрации:**

Ваш адрес будет выглядеть так:
```
http://abcdef123456.onion
```

**3. Как открыть .onion сайт:**

- **Tor Browser**: https://www.torproject.org/download/
- Установите и откройте ваш .onion адрес
- Работает на любом устройстве (Windows, Mac, Linux, Android)

### ⚠️ Важно:

- .onion адрес генерируется при первом запуске Tor
- Адрес не меняется (сохраняется в `/tmp/tor_interceptor/hidden_service/`)
- Для доступа нужен Tor Browser или Tor-прокси
- Первое подключение может занять 30-60 секунд

---

## ☁️ Метод 2: Cloudflare Tunnel (Быстро и просто)

### ✅ Преимущества:
- Бесплатно
- Не требует публичного IP
- HTTPS из коробки
- Защита от DDoS
- Обычный домен (не .onion)

### 🚀 Быстрый старт:

**Вариант A: Временный туннель (без регистрации)**

```bash
cd ~/web-interogatter/raspberry-production
chmod +x setup_cloudflare_tunnel.sh
./setup_cloudflare_tunnel.sh
```

Выберите "Yes" для запуска быстрого туннеля. Получите временную ссылку вида:
```
https://random-words-1234.trycloudflare.com
```

**Вариант B: Постоянный туннель (с регистрацией)**

1. Зарегистрируйтесь на https://dash.cloudflare.com/
2. Перейдите в **Zero Trust > Access > Tunnels**
3. Создайте новый туннель
4. Установите cloudflared (скрипт сделает это автоматически)
5. Запустите с вашим токеном

### 📋 Команды:

```bash
# Установка
./setup_cloudflare_tunnel.sh

# Быстрый туннель
cloudflared tunnel --url http://localhost:5000

# С токеном (постоянный)
cloudflared tunnel --url http://localhost:5000 run <YOUR_TOKEN>
```

---

## 🔐 Метод 3: Tailscale VPN (Самый безопасный)

### ✅ Преимущества:
- Приватная VPN сеть
- Шифрование end-to-end
- Не требует публичного IP
- Работает через NAT
- Бесплатно до 100 устройств
- Доступ только для ваших устройств

### 🚀 Быстрый старт:

**1. Установка на Raspberry Pi:**

```bash
cd ~/web-interogatter/raspberry-production
chmod +x setup_tailscale.sh
./setup_tailscale.sh
```

**2. Установка на вашем устройстве:**

- **Windows/Mac/Linux**: https://tailscale.com/download
- **iPhone/Android**: Приложение из App Store / Play Store

**3. Войдите с одним аккаунтом на обоих устройствах**

**4. Получите Tailscale IP Raspberry Pi:**

```bash
tailscale ip -4
```

**5. Откройте в браузере:**

```
http://<TAILSCALE_IP>:5000
```

### 📝 Пример:

```bash
# На Raspberry Pi
tailscale ip -4
# Вывод: 100.64.1.5

# На вашем компьютере/телефоне откройте:
http://100.64.1.5:5000
```

---

## 🌍 Метод 4: Port Forwarding (Проброс портов)

### ⚠️ Внимание:
- Требует доступ к настройкам роутера
- Открывает порт в интернет (небезопасно без дополнительной защиты)
- Требует статический или динамический DNS
- **Не рекомендуется без дополнительной защиты**

### 🚀 Настройка:

**1. Узнайте локальный IP Raspberry Pi:**

```bash
hostname -I
# Пример: 192.168.1.100
```

**2. Настройте Port Forwarding на роутере:**

Зайдите в настройки роутера (обычно http://192.168.1.1 или http://192.168.0.1):

- **External Port**: 8080 (или любой свободный)
- **Internal IP**: `<IP Raspberry Pi>` (например, 192.168.1.100)
- **Internal Port**: 5000
- **Protocol**: TCP

**3. Узнайте ваш публичный IP:**

```bash
curl ifconfig.me
# или
curl icanhazip.com
```

**4. Доступ извне:**

```
http://<ВАШ_ПУБЛИЧНЫЙ_IP>:8080
```

### 🔒 Дополнительная защита (рекомендуется):

**Установите nginx с базовой аутентификацией:**

```bash
sudo apt install nginx apache2-utils

# Создайте пароль
sudo htpasswd -c /etc/nginx/.htpasswd admin

# Настройте nginx reverse proxy
sudo nano /etc/nginx/sites-available/interceptor
```

Конфиг nginx:

```nginx
server {
    listen 80;
    
    auth_basic "Restricted Access";
    auth_basic_user_file /etc/nginx/.htpasswd;
    
    location / {
        proxy_pass http://localhost:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активируйте:

```bash
sudo ln -s /etc/nginx/sites-available/interceptor /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🆚 Сравнение методов

| Метод | Сложность | Безопасность | Скорость | Стоимость | Рекомендация |
|-------|-----------|--------------|----------|-----------|--------------|
| **Tor Hidden Service** | ⭐ Легко | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | 🆓 | ✅ **Лучший для кибербезопасности** |
| **Cloudflare Tunnel** | ⭐⭐ Легко | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🆓 | ✅ **Лучший для демонстрации** |
| **Tailscale VPN** | ⭐⭐ Средне | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 🆓 | ✅ **Лучший для приватного доступа** |
| **Port Forwarding** | ⭐⭐⭐ Сложно | ⭐⭐ | ⭐⭐⭐⭐⭐ | 🆓 | ⚠️ **Требует защиту** |

---

## 🎯 Рекомендации по использованию

### Для демонстрации проекта в школе/на конференции:
1. **Tor Hidden Service** - показывает знание анонимности и Dark Web
2. **Cloudflare Tunnel** - быстрый и профессиональный способ

### Для личного тестирования:
- **Tailscale** - безопасный доступ со всех ваших устройств

### Для продакшена (если нужно):
- **Cloudflare Tunnel** + дополнительная аутентификация
- Или **Tailscale** для приватных клиентов

---

## 🔧 Полезные команды

### Проверка статуса сервисов:

```bash
# Статус приложения
./raspberry-run.sh status

# Tor Hidden Service адрес
./raspberry-run.sh onion

# Логи
./raspberry-run.sh logs

# Tailscale статус
sudo tailscale status

# Публичный IP
curl ifconfig.me
```

### Управление firewall (если нужно):

```bash
# Открыть порт 5000
sudo ufw allow 5000/tcp

# Проверить статус
sudo ufw status
```

---

## 📱 Доступ с мобильного устройства

### Tor (Android/iOS):
- Android: **Tor Browser** из Play Store
- iOS: **Onion Browser** из App Store

### Tailscale (Android/iOS):
- Установите приложение Tailscale
- Войдите с тем же аккаунтом
- Откройте http://<TAILSCALE_IP>:5000

### Cloudflare Tunnel:
- Просто откройте ссылку в любом браузере

---

## 🆘 Troubleshooting

### Tor не создает .onion адрес:

```bash
# Проверьте логи Tor
tail -f /tmp/tor_interceptor/tor.log

# Перезапустите Tor
./raspberry-run.sh restart

# Подождите 60-90 секунд и проверьте снова
./raspberry-run.sh onion
```

### Не могу подключиться через Cloudflare:

```bash
# Проверьте, что Flask работает
curl http://localhost:5000

# Перезапустите туннель
cloudflared tunnel --url http://localhost:5000
```

### Tailscale не подключается:

```bash
# Проверьте статус
sudo tailscale status

# Переподключение
sudo tailscale up

# Проверьте IP
tailscale ip -4
```

---

## 📚 Дополнительные ресурсы

- **Tor Project**: https://www.torproject.org/
- **Cloudflare Tunnel**: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/
- **Tailscale**: https://tailscale.com/kb/
- **Port Forwarding гайды**: https://portforward.com/

---

## 🎓 Для проекта по кибербезопасности

**Рекомендую использовать Tor Hidden Service** потому что:

1. ✅ Демонстрирует понимание анонимности и Dark Web
2. ✅ Показывает знание Tor и луковой маршрутизации
3. ✅ Не требует дополнительной инфраструктуры
4. ✅ Работает из любой точки мира
5. ✅ Невозможно отследить местоположение сервера

**Как презентовать:**

```
"Я развернул Web Server Interceptor на Raspberry Pi с 
использованием Tor Hidden Service для анонимного доступа.
Это демонстрирует знание:
- Tor и луковой маршрутизации
- Настройки hidden services
- Безопасного хостинга в Dark Web
- Защиты локальной инфраструктуры"
```

🎯 **Ваш .onion адрес - это ваша визитная карточка проекта!**

