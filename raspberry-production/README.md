# 🍓 Raspberry Pi - Production версия

Версия для запуска на **Raspberry Pi 4** с **публичным доступом** в открытом интернете и **усиленной защитой**.

## 📋 Описание

Эта версия оптимизирована для:
- ✅ Production сервера в открытом интернете
- ✅ Усиленной безопасности
- ✅ Автоматического мониторинга
- ✅ Резервного копирования
- ✅ Защиты от атак

## 🔒 Компоненты безопасности

- **fail2ban** - Защита от брутфорса
- **Усиленный firewall** - Строгие правила UFW
- **Автоматические обновления** - Безопасность системы
- **Мониторинг безопасности** - Логирование и алерты
- **Rate limiting** - Ограничение запросов
- **Автоматическое резервное копирование**

## 🚀 Быстрый старт

### ⚡ Автоматическая установка (рекомендуется)

**Для первого запуска или обновления из git:**

```bash
# Если проект уже клонирован
cd ~/web-server-intercepter/raspberry-production
./install_and_run.sh

# Или с указанием git URL
./install_and_run.sh https://github.com/YOUR_USERNAME/web-server-intercepter.git
```

Скрипт автоматически:
- ✅ Обновит проект из git (или клонирует, если нет)
- ✅ Установит все зависимости (Python пакеты, Tor)
- ✅ Настроит окружение
- ✅ Запустит сервер (БЕЗ Docker - напрямую через Python)

**Подробная инструкция:** [INSTALL.md](INSTALL.md)

---

### 📋 Ручная установка

#### 1. Первоначальная настройка

```bash
# Автоматическая настройка системы
./setup_raspberry.sh

# Усиленная настройка безопасности (требует sudo)
sudo ./setup_security.sh
```

#### 2. Настройка firewall

```bash
# Усиленная настройка firewall для production
sudo ./setup_production_firewall.sh 5000
```

⚠️ **ВНИМАНИЕ**: Убедитесь, что SSH доступен перед настройкой firewall!

#### 3. Запуск приложения

```bash
# Запуск основных сервисов
./raspberry-run.sh start

# Проверка статуса
./raspberry-run.sh status

# Мониторинг безопасности
./monitor_security.sh
```

## 📁 Файлы в этой папке

### Основные скрипты

- **`install_and_run.sh`** - 🚀 **Автоматическая установка и запуск** (клонирование из git, настройка, запуск)
- **`raspberry-run.sh`** - Управление приложением
- **`setup_raspberry.sh`** - Автоматическая настройка системы

### Скрипты безопасности

- **`setup_security.sh`** - Полная настройка безопасности (fail2ban, обновления, мониторинг)
- **`setup_production_firewall.sh`** - Усиленная настройка firewall
- **`monitor_security.sh`** - Мониторинг безопасности

## 🔒 Настройка безопасности

### Полная настройка безопасности

```bash
# Запуск полной настройки (требует sudo)
sudo ./setup_security.sh
```

Этот скрипт настроит:
- ✅ fail2ban для защиты от брутфорса
- ✅ Усиленный firewall с ограничениями
- ✅ Автоматические обновления безопасности
- ✅ Логирование безопасности
- ✅ Ограничения ресурсов
- ✅ Мониторинг системы
- ✅ Автоматическое резервное копирование

### Настройка firewall

```bash
# Усиленная настройка firewall
sudo ./setup_production_firewall.sh 5000
```

Firewall настроит:
- Разрешение SSH (порт 22)
- Разрешение HTTP/HTTPS (порты 80/443)
- Приложение только для локальной сети
- Tor порты только для localhost
- Rate limiting для SSH

### Мониторинг безопасности

```bash
# Просмотр статуса безопасности
./monitor_security.sh
```

Показывает:
- Статус firewall
- Статус fail2ban
- Неудачные попытки входа
- Логи безопасности
- Статус системы
- Резервные копии

## 🎮 Управление приложением

### Запуск и остановка

```bash
# Запуск сервисов (Tor + Flask)
./raspberry-run.sh start

# Остановка
./raspberry-run.sh stop

# Перезапуск
./raspberry-run.sh restart
```

### Мониторинг

```bash
# Статус контейнеров
./raspberry-run.sh status

# Логи
./raspberry-run.sh logs
./raspberry-run.sh logs flask
./raspberry-run.sh logs tor
./raspberry-run.sh logs interceptor

# Мониторинг ресурсов Raspberry Pi
./raspberry-run.sh monitor

# Мониторинг безопасности
./monitor_security.sh
```

### Tor управление

```bash
# Получить .onion адрес
./raspberry-run.sh onion

# Сменить Tor идентичность
./raspberry-run.sh newip
```

## 🌐 Доступ к интерфейсам

### Локальный доступ

- **Основной сайт**: `http://localhost:5000`
- **Админ панель**: `http://localhost:5000/admin/reports`

### Сетевой доступ

```bash
# Узнать IP адрес
hostname -I

# Открыть в браузере
http://[IP-адрес]:5000
```

### Tor Hidden Service

```bash
# Получить .onion адрес
./raspberry-run.sh onion

# Открыть в Tor Browser
http://[onion-адрес]
```

## 📊 Логи и мониторинг

### Логи безопасности

```bash
# Логи безопасности
tail -f /var/log/security-monitor/security.log

# Логи системы
tail -f /var/log/security-monitor/system.log

# Логи firewall
tail -f /var/log/ufw.log

# Логи fail2ban
tail -f /var/log/fail2ban.log
```

### Резервные копии

```bash
# Просмотр резервных копий
ls -lh /var/backups/web-interceptor/

# Восстановление из резервной копии
tar -xzf /var/backups/web-interceptor/data_YYYYMMDD_HHMMSS.tar.gz
```

## 🔧 Обслуживание

### Обновление системы

```bash
# Ручное обновление
sudo apt update && sudo apt upgrade -y

# Проверка автоматических обновлений
systemctl status unattended-upgrades
```

### Обновление приложения

```bash
# Обновление из git
cd ~/web-interogatter
git pull origin main

# Перезапуск сервисов
cd raspberry-production
./raspberry-run.sh restart
```

### Очистка

```bash
# Очистка временных файлов
./raspberry-run.sh cleanup

# Очистка старых логов
find logs -name "*.log.*" -mtime +30 -delete
sudo find /var/log -name "*.log" -mtime +30 -delete
```

## ⚙️ Требования

- **Raspberry Pi 4** (рекомендуется 4GB RAM или больше)
- **Raspberry Pi OS (64-bit)**
- **Python 3.7+**
- **Tor**
- **Стабильное интернет-соединение**

> **Примечание**: Эта версия работает **БЕЗ Docker** - запуск напрямую через Python для лучшей производительности на Raspberry Pi.

## 🛡️ Рекомендации по безопасности

1. **Измените пароль по умолчанию**
   ```bash
   passwd
   ```

2. **Настройте SSH ключи** (вместо паролей)
   ```bash
   ssh-keygen -t ed25519
   ```

3. **Используйте Tor Hidden Service** для публичного доступа
   ```bash
   ./raspberry-run.sh onion
   ```

4. **Регулярно проверяйте логи безопасности**
   ```bash
   ./monitor_security.sh
   ```

5. **Делайте резервные копии**
   - Автоматические копии: `/var/backups/web-interceptor/`
   - Ручные копии: `./raspberry-run.sh export`

6. **Мониторьте систему**
   ```bash
   ./raspberry-run.sh monitor
   ```

## 🐛 Решение проблем

### Firewall блокирует доступ

```bash
# Проверка статуса
sudo ufw status

# Временное отключение (только для отладки!)
sudo ufw disable

# Просмотр логов
sudo tail -f /var/log/ufw.log
```

### fail2ban блокирует IP

```bash
# Просмотр заблокированных IP
sudo fail2ban-client status web-interceptor

# Разблокировка IP
sudo fail2ban-client set web-interceptor unbanip [IP-адрес]
```

### Высокая нагрузка

```bash
# Мониторинг ресурсов
./raspberry-run.sh monitor

# Проверка процессов
ps aux | grep -E "tor|app.py"

# Ограничение ресурсов через systemd (если используется)
# См. документацию по systemd resource limits
```

## 📝 Дополнительная документация

- **Основной README**: `../README.md`
- **Установка на Raspberry Pi**: `../gu/information/RASPBERRY_PI_SETUP.md`
- **Логирование**: `../gu/information/DOCKER_AND_LOGGING.md` (применимо и без Docker)

## ⚠️ Важные предупреждения

1. ⚠️ **Это production версия** - используйте только в контролируемой среде
2. ⚠️ **Убедитесь, что SSH доступен** перед настройкой firewall
3. ⚠️ **Регулярно проверяйте логи** безопасности
4. ⚠️ **Используйте Tor Hidden Service** для публичного доступа
5. ⚠️ **Соблюдайте законы** вашей юрисдикции

---

**🎓 Образовательный проект по кибербезопасности**  
*Используйте ответственно и в соответствии с законами*

