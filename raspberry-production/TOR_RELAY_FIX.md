# 🔧 Исправление ошибки с Tor Relay на Raspberry Pi

## Проблема

При запуске сервиса возникает ошибка:
```
Error response from daemon: image with reference peterdavehello/tor-socks-proxy:latest 
was found but does not provide the specified platform (linux/arm64)
```

## Причина

Образ `peterdavehello/tor-socks-proxy:latest` не поддерживает архитектуру ARM64 (Raspberry Pi).

## ✅ Решение

**Хорошая новость**: Tor уже встроен в основной контейнер `interceptor`! 

Сервис `tor-relay` был дополнительным и не обязателен для работы приложения.

### Что было исправлено:

1. ✅ Убрана зависимость `depends_on: tor-relay` из основного контейнера
2. ✅ Сервис `tor-relay` сделан опциональным (через profiles)
3. ✅ Функция `start_basic()` теперь запускает только `interceptor`

## 🚀 Запуск

Теперь можно запустить сервис без ошибок:

```bash
cd ~/web-interogatter/raspberry-production
./raspberry-run.sh start
```

Это запустит только основной контейнер `interceptor`, который уже содержит:
- ✅ Flask веб-сервер
- ✅ Tor SOCKS прокси (порт 9050)
- ✅ Tor Control порт (порт 9051)
- ✅ Tor Hidden Service (.onion адрес)

## 📋 Что доступно

После запуска будут доступны:

- **Веб-сервер**: http://localhost:5000
- **Tor SOCKS прокси**: 127.0.0.1:9050
- **Tor Control**: 127.0.0.1:9051
- **Tor Hidden Service**: (получить через `./raspberry-run.sh onion`)

## 🔄 Если нужен дополнительный Tor прокси

Если вам действительно нужен отдельный Tor контейнер (например, для дополнительного прокси на порту 9150), вы можете:

### Вариант 1: Использовать другой образ с поддержкой ARM64

Найдите образ Tor, который поддерживает ARM64, или соберите свой.

### Вариант 2: Использовать встроенный Tor

Встроенный Tor в контейнере `interceptor` уже предоставляет все необходимые функции:
- SOCKS прокси на порту 9050
- Control порт на 9051
- Hidden Service

### Вариант 3: Запустить Tor на хосте

Установите Tor на сам Raspberry Pi:

```bash
sudo apt update
sudo apt install -y tor
sudo systemctl start tor
```

Tor будет доступен на `127.0.0.1:9050` на хосте.

## ✅ Проверка работы

После запуска проверьте:

```bash
# Статус контейнеров
./raspberry-run.sh status

# Логи
./raspberry-run.sh logs

# Получить .onion адрес
./raspberry-run.sh onion

# Проверить Tor прокси
curl --socks5 127.0.0.1:9050 https://check.torproject.org/api/ip
```

## 📝 Примечание

Все функции приложения работают без отдельного контейнера `tor-relay`. Tor полностью интегрирован в основной контейнер и запускается автоматически при старте приложения.
