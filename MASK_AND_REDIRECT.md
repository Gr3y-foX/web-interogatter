# 🎭 Маскировочный сайт и система перехвата

Полная система с маскировочным сайтом, перенаправлением через Tor и шуточным отчетом о перехвате.

## 🎯 Как это работает

1. **Маскировочный сайт** - выглядит как обычный новостной сайт
2. **Автоматическое перенаправление** - через 2-3 секунды перенаправляет на страницу перехвата
3. **Сбор данных** - собирает всю информацию о клиенте
4. **Шуточный отчет** - показывает пользователю "отчет о перехвате" в стиле хакера

## 🚀 Быстрый старт

### 1. Запуск сервера

```bash
# Запуск всех сервисов
./run.sh start

# Настройка Tor перенаправления
./setup_tor_redirect.sh
```

### 2. Доступ к маскировочному сайту

#### Через HTTP (локально)
```
http://localhost:5000/mask
http://localhost:5000/?mode=mask
```

#### Через Tor (.onion)
```
http://[onion-address].onion/mask
```

### 3. Страница перехвата

```
http://localhost:5000/intercept
http://[onion-address].onion/intercept
```

## 📋 Структура системы

### Маскировочный сайт (`/mask`)

Выглядит как обычный новостной сайт:
- Заголовок с навигацией
- Статьи о технологиях
- Боковая панель с популярными статьями
- Форма подписки

**Особенности:**
- Автоматическое перенаправление через 2-3 секунды
- Перехват всех кликов по ссылкам
- Сбор данных при попытке подписки

### Страница перехвата (`/intercept`)

Шуточная страница в стиле "хакера":
- Эффект матрицы на фоне
- Анимированный отчет о перехвате
- Отображение всех собранных данных:
  - IP адрес
  - Браузер и ОС
  - Цифровой отпечаток (fingerprint)
  - Session ID
  - Время перехвата

### Система перенаправления

Все ссылки на маскировочном сайте перенаправляют на `/intercept`:
- `/article/*` → `/intercept?ref=article`
- `/tech`, `/ai`, `/security` → `/intercept?ref=category`
- Форма подписки → `/intercept?ref=subscribe`
- Любые другие ссылки → `/intercept?ref=[path]`

## 🔧 Настройка

### Изменение времени перенаправления

В `templates/mask_site.html` измените:

```javascript
setTimeout(function() {
    window.location.href = '/intercept?ref=mask_site';
}, 2000 + Math.random() * 1000); // 2-3 секунды
```

### Изменение дизайна маскировочного сайта

Отредактируйте `templates/mask_site.html`:
- Измените название сайта
- Добавьте свои статьи
- Измените цветовую схему
- Добавьте больше контента

### Изменение отчета о перехвате

Отредактируйте `templates/caught_report.html`:
- Измените текст предупреждений
- Добавьте больше информации
- Измените стиль (цвета, анимации)

## 🧅 Использование через Tor

### 1. Получение .onion адреса

```bash
python3 view_logs.py onion
# или
python3 tor_setup.py hidden
```

### 2. Публикация маскировочного сайта

Используйте .onion адрес для доступа:
```
http://[onion-address].onion/mask
```

### 3. Преимущества Tor

- ✅ Полная анонимность
- ✅ Не требует открытия портов
- ✅ Работает из любой сети
- ✅ Защита от DDoS

## 📊 Сбор данных

Система автоматически собирает:

### Базовая информация
- IP адрес
- User-Agent
- Браузер и версия
- Операционная система
- Тип устройства

### Расширенная информация
- Browser fingerprint
- Session ID
- Cookies
- Screen resolution
- Timezone
- Referer
- Все HTTP заголовки

### Сохранение данных

Все данные сохраняются:
- В SQLite базу данных (`data/intercepts.db`)
- В JSON файлы (`reports/intercept_*.json`)
- В логи (`logs/intercepts.log`)

## 🎨 Кастомизация

### Создание своего маскировочного сайта

1. Создайте новый шаблон в `templates/`
2. Добавьте маршрут в `app.py`:

```python
@app.route('/my-mask')
def my_mask():
    client_info = get_client_info(request)
    threading.Thread(target=save_intercept, args=(client_info,)).start()
    return render_template('my_mask.html'), 200
```

3. Добавьте перенаправление в JavaScript:

```javascript
setTimeout(() => {
    window.location.href = '/intercept?ref=my_mask';
}, 2000);
```

### Создание своего отчета

1. Создайте новый шаблон в `templates/`
2. Обновите маршрут `/intercept`:

```python
@app.route('/intercept')
def intercept_page():
    client_info = get_client_info(request)
    intercept_id = save_intercept(client_info)
    return render_template('my_report.html', intercept_data=client_info), 200
```

## 🔍 Просмотр перехваченных данных

### Через админ панель
```
http://localhost:5000/admin/reports
```

### Через API
```bash
curl http://localhost:5000/admin/api/reports | jq
```

### Через утилиту
```bash
python3 view_logs.py intercepts 50
python3 view_logs.py stats
```

## ⚠️ Важные замечания

### Безопасность
- Используйте только в изолированной среде
- Не используйте на реальных пользователях без разрешения
- Все данные собираются только для образовательных целей

### Легальность
- Убедитесь, что использование соответствует законам вашей страны
- Получайте разрешение перед тестированием
- Используйте только в рамках учебных проектов

### Этичность
- Информируйте пользователей о сборе данных (если возможно)
- Не используйте для вредоносных целей
- Уважайте приватность пользователей

## 📝 Примеры использования

### Сценарий 1: Образовательная демонстрация

1. Запустите сервер
2. Откройте маскировочный сайт
3. Покажите как работает перенаправление
4. Продемонстрируйте собранные данные

### Сценарий 2: Тестирование безопасности

1. Настройте через Tor
2. Поделитесь .onion адресом
3. Соберите данные о подключениях
4. Проанализируйте результаты

### Сценарий 3: Демонстрация приватности

1. Покажите маскировочный сайт
2. Продемонстрируйте сбор данных
3. Объясните важность защиты приватности
4. Покажите рекомендации по безопасности

## 🐛 Решение проблем

### Маскировочный сайт не перенаправляет

Проверьте JavaScript в консоли браузера (F12):
```javascript
// Должно быть перенаправление через 2-3 секунды
```

### Данные не сохраняются

Проверьте логи:
```bash
tail -f logs/interceptor.log
tail -f logs/errors.log
```

### Tor не работает

```bash
python3 tor_setup.py check
python3 tor_setup.py start
```

---

**🎓 Образовательный проект по кибербезопасности**

*Используйте ответственно и в соответствии с законами!*

