# 🐧 Kali Linux - Локальная версия

Версия для запуска на **Kali Linux** с **локальным доступом** (localhost/локальная сеть).

## 📋 Описание

Эта версия оптимизирована для:
- ✅ Локального использования (localhost)
- ✅ Образовательных целей
- ✅ Тестирования в изолированной среде
- ✅ Быстрого запуска без дополнительной настройки

## 🚀 Быстрый старт

### Вариант 1: Прямой запуск (без Docker)

```bash
# Установка зависимостей и запуск
./run.sh start

# Проверка статуса
./run.sh status

# Просмотр логов
./run.sh logs

# Остановка
./run.sh stop
```

### Вариант 2: Docker

```bash
# Запуск с Docker Compose
./docker-run.sh start

# Просмотр статуса
./docker-run.sh status

# Просмотр логов
./docker-run.sh logs

# Остановка
./docker-run.sh stop
```

## 📁 Файлы в этой папке

- **`run.sh`** - Основной скрипт запуска (без Docker)
- **`docker-run.sh`** - Скрипт управления Docker контейнерами
- **`setup_tor_redirect.sh`** - Настройка перенаправления через Tor

## 🎯 Использование

### Запуск сервисов

```bash
# Запуск всех сервисов
./run.sh start

# Или с Docker
./docker-run.sh start
```

### Доступ к интерфейсам

После запуска сервисы будут доступны:

- **Основной сайт**: `http://localhost:5000`
- **Маскировочный сайт**: `http://localhost:5000/mask`
- **Страница перехвата**: `http://localhost:5000/intercept`
- **Админ панель**: `http://localhost:5000/admin/reports`

### Локальная сеть

Для доступа из локальной сети:

```bash
# Узнать IP адрес
hostname -I

# Открыть в браузере
http://[ваш-IP]:5000
```

### Tor Hidden Service

```bash
# Настройка Tor перенаправления
./setup_tor_redirect.sh

# Получение .onion адреса
python3 ../tor_setup.py hidden
```

## 🔧 Команды

### run.sh

```bash
./run.sh start      # Запуск сервисов
./run.sh stop       # Остановка сервисов
./run.sh restart    # Перезапуск
./run.sh status     # Статус сервисов
./run.sh logs       # Просмотр логов
./run.sh cleanup    # Очистка данных
./run.sh install    # Установка зависимостей
```

### docker-run.sh

```bash
./docker-run.sh start              # Запуск основных сервисов
./docker-run.sh start-full         # Запуск всех сервисов
./docker-run.sh start-monitoring   # Запуск с мониторингом
./docker-run.sh stop               # Остановка
./docker-run.sh status             # Статус
./docker-run.sh logs [service]     # Логи
./docker-run.sh onion              # Получить .onion адрес
```

## ⚙️ Требования

- **Kali Linux** (или другой Debian-based дистрибутив)
- **Python 3.7+**
- **Tor**
- **Docker** (опционально, для Docker версии)

## 🔒 Безопасность

⚠️ **ВАЖНО**: Эта версия предназначена для **локального использования**!

- Не открывает порты в интернет
- Минимальная конфигурация безопасности
- Подходит для изолированной среды (VM)

Для production использования с публичным доступом используйте версию из папки `raspberry-production/`.

## 📝 Примечания

- Все данные сохраняются в директории `../data/`
- Логи находятся в `../logs/`
- Отчеты сохраняются в `../reports/`

## 🆘 Решение проблем

### Порт 5000 занят

```bash
# Найти процесс
sudo lsof -i :5000

# Остановить процесс
kill -9 [PID]
```

### Tor не запускается

```bash
# Проверка установки
tor --version

# Установка Tor
sudo apt update && sudo apt install tor

# Запуск Tor
sudo systemctl start tor
```

### Docker проблемы

```bash
# Проверка Docker
docker --version
docker ps

# Перезапуск Docker
sudo systemctl restart docker
```

---

**🎓 Образовательный проект по кибербезопасности**  
*Используйте ответственно и в соответствии с законами*

