# 🐳 Сборка единого Docker контейнера для Kali Linux

Инструкция по сборке и запуску единого Docker контейнера, который включает все компоненты проекта.

## 📋 Что включено в контейнер

Единый контейнер включает:
- ✅ **Flask приложение** (app.py)
- ✅ **Tor** (SOCKS прокси и Hidden Service)
- ✅ **Все скрипты** из kali-local/
- ✅ **Шаблоны и локализация**
- ✅ **Конфигурация** (torrc, nginx.conf)
- ✅ **Все зависимости** (Python пакеты, системные утилиты)

## 🚀 Быстрый старт

### Автоматическая сборка и запуск

```bash
cd kali-local
./docker-build-and-run.sh build-and-run
```

Это автоматически:
1. Соберет Docker образ
2. Запустит контейнер
3. Покажет URL для доступа

## 📖 Детальная инструкция

### Шаг 1: Сборка образа

```bash
# Переход в директорию скриптов
cd kali-local

# Сборка образа
./docker-build-and-run.sh build

# Или сборка без кэша (для чистой сборки)
./docker-build-and-run.sh build --no-cache
```

### Шаг 2: Запуск контейнера

```bash
# Запуск контейнера
./docker-build-and-run.sh start

# Или одной командой (сборка + запуск)
./docker-build-and-run.sh build-and-run
```

### Шаг 3: Проверка работы

```bash
# Статус контейнера
./docker-build-and-run.sh status

# Просмотр логов
./docker-build-and-run.sh logs

# Получение .onion адреса
./docker-build-and-run.sh onion
```

## 🎮 Управление контейнером

### Основные команды

```bash
# Запуск
./docker-build-and-run.sh start

# Остановка
./docker-build-and-run.sh stop

# Перезапуск
./docker-build-and-run.sh restart

# Статус
./docker-build-and-run.sh status

# Логи
./docker-build-and-run.sh logs

# URL адреса
./docker-build-and-run.sh urls

# .onion адрес
./docker-build-and-run.sh onion

# Интерактивная оболочка
./docker-build-and-run.sh shell
```

### Ручное управление через Docker

```bash
# Запуск
docker-compose -f ../docker-compose.kali.yml up -d

# Остановка
docker-compose -f ../docker-compose.kali.yml down

# Логи
docker logs web-interceptor-kali

# Вход в контейнер
docker exec -it web-interceptor-kali /bin/bash
```

## 🌐 Доступ к сервисам

После запуска контейнера сервисы доступны:

- **Основной сайт**: `http://localhost:5000`
- **Админ панель**: `http://localhost:5000/admin/reports`
- **API**: `http://localhost:5000/admin/api/reports`
- **Tor SOCKS**: `127.0.0.1:9050`
- **Tor Control**: `127.0.0.1:9051`
- **Tor Hidden Service**: `http://[onion-адрес]` (получить: `./docker-build-and-run.sh onion`)

## 📁 Структура файлов

```
web-server-intercepter/
├── Dockerfile.kali              # Dockerfile для единого контейнера
├── docker-compose.kali.yml      # Docker Compose конфигурация
├── kali-local/
│   ├── docker-build-and-run.sh  # Скрипт сборки и запуска
│   └── ...
└── ...
```

## 🔧 Настройка

### Изменение портов

Отредактируйте `docker-compose.kali.yml`:

```yaml
ports:
  - "8080:5000"    # Изменить внешний порт
```

### Изменение переменных окружения

Отредактируйте `docker-compose.kali.yml`:

```yaml
environment:
  - FLASK_ENV=development
  - LOG_LEVEL=DEBUG
```

## 🐛 Решение проблем

### Проблема: Образ не собирается

```bash
# Очистка и пересборка
./docker-build-and-run.sh cleanup
./docker-build-and-run.sh build --no-cache
```

### Проблема: Контейнер не запускается

```bash
# Просмотр логов
./docker-build-and-run.sh logs

# Проверка статуса
docker ps -a | grep web-interceptor-kali

# Проверка портов
netstat -tuln | grep -E ":(5000|9050|9051)"
```

### Проблема: Порт занят

```bash
# Найти процесс
sudo lsof -i :5000

# Остановить процесс
sudo kill -9 [PID]

# Или изменить порт в docker-compose.kali.yml
```

### Проблема: Tor не создает Hidden Service

```bash
# Проверка логов Tor
docker exec web-interceptor-kali cat /app/logs/tor.log

# Проверка директории
docker exec web-interceptor-kali ls -la /var/lib/tor-interceptor/hidden_service/

# Перезапуск контейнера
./docker-build-and-run.sh restart
```

## 📊 Мониторинг

### Использование ресурсов

```bash
# Статус контейнера
./docker-build-and-run.sh status

# Детальная статистика
docker stats web-interceptor-kali
```

### Логи

```bash
# Все логи
./docker-build-and-run.sh logs

# Логи приложения
docker exec web-interceptor-kali tail -f /app/logs/interceptor.log

# Логи Tor
docker exec web-interceptor-kali tail -f /app/logs/tor.log
```

## 🔄 Обновление

### Обновление образа

```bash
# Остановка контейнера
./docker-build-and-run.sh stop

# Пересборка образа
./docker-build-and-run.sh build --no-cache

# Запуск
./docker-build-and-run.sh start
```

### Обновление кода

```bash
# Обновление кода в репозитории
git pull

# Пересборка и перезапуск
./docker-build-and-run.sh build-and-run
```

## 💾 Данные

Данные сохраняются в Docker volumes:
- **База данных**: `interceptor_data` → `/app/data`
- **Отчеты**: `interceptor_reports` → `/app/reports`
- **Логи**: `interceptor_logs` → `/app/logs`
- **Tor данные**: `tor_data` → `/var/lib/tor-interceptor`

### Резервное копирование

```bash
# Экспорт данных
docker run --rm -v interceptor_data:/data -v $(pwd):/backup \
  alpine tar czf /backup/backup.tar.gz -C /data .

# Восстановление
docker run --rm -v interceptor_data:/data -v $(pwd):/backup \
  alpine tar xzf /backup/backup.tar.gz -C /data
```

## ⚠️ Важные замечания

1. ⚠️ **Первый запуск** может занять несколько минут (сборка образа)
2. ⚠️ **Tor Hidden Service** создается в течение 1-2 минут после запуска
3. ⚠️ **Данные сохраняются** в Docker volumes, даже после удаления контейнера
4. ⚠️ **Порты 5000, 9050, 9051** должны быть свободны

---

**🎓 Образовательный проект по кибербезопасности**  
*Используйте ответственно и в соответствии с законами*

