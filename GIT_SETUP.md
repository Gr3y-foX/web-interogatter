# 📦 Git Repository Setup

## ✅ Что уже сделано

1. ✅ Git репозиторий инициализирован
2. ✅ Создан `.gitignore` для исключения ненужных файлов
3. ✅ Создан первый commit с базовой структурой проекта
4. ✅ Добавлена поддержка локализации (EN/RU)

## 📋 Структура локализации

```
templates/
├── en/              # English templates
│   └── mask_site.html
├── ru/              # Russian templates
│   ├── mask_site.html
│   ├── caught_report.html
│   ├── error.html
│   └── admin.html
└── *.html           # Default templates (legacy)

locales/
├── en.json          # English translations
└── ru.json          # Russian translations
```

## 🚀 Как сделать push в удаленный репозиторий

### Вариант 1: GitHub

```bash
# 1. Создайте репозиторий на GitHub (через веб-интерфейс)

# 2. Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/web-server-interceptor.git

# 3. Переименуйте ветку в main (если нужно)
git branch -M main

# 4. Сделайте push
git push -u origin main
```

### Вариант 2: GitLab

```bash
# 1. Создайте репозиторий на GitLab

# 2. Добавьте remote
git remote add origin https://gitlab.com/YOUR_USERNAME/web-server-interceptor.git

# 3. Сделайте push
git push -u origin master
```

### Вариант 3: Другой Git сервер

```bash
# Добавьте remote
git remote add origin YOUR_GIT_URL

# Сделайте push
git push -u origin master
```

## 📝 Текущий статус

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения
git diff
```

## 🔄 Дальнейшая работа

### Добавление изменений

```bash
# Добавить файлы
git add .

# Создать commit
git commit -m "Описание изменений"

# Отправить на сервер
git push
```

### Создание веток

```bash
# Создать новую ветку
git checkout -b feature/new-feature

# Переключиться на ветку
git checkout master

# Слить ветку
git merge feature/new-feature
```

## 📋 Что нужно перевести (TODO)

### Шаблоны (templates/)
- [ ] `caught_report.html` - английская версия
- [ ] `error.html` - английская версия  
- [ ] `admin.html` - английская версия

### Сообщения в app.py
- [ ] Логи и сообщения в консоли
- [ ] Комментарии в коде

### Документация
- [ ] Все .md файлы на английский
- [ ] Комментарии в скриптах

## 🌍 Использование локализации

### В URL
Добавьте параметр `?lang=en` или `?lang=ru`:
- http://localhost:5000/mask?lang=en
- http://localhost:5000/intercept?lang=ru

### По умолчанию
Язык по умолчанию - английский (en)

## ⚠️ Важно

- Не коммитьте файлы с чувствительными данными
- `.gitignore` уже настроен для исключения:
  - Базы данных (*.db, *.sqlite)
  - Логи (*.log)
  - Данные Tor
  - Временные файлы
  - Виртуальные окружения

---

**Готово к push!** 🚀

